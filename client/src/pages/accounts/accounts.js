import React from "react";

import Layout from "../../components/layout";

const Accounts = ({ plaidLink }) => {
  return (
    <Layout selected="accounts" plaidLink={plaidLink}>
      <div>TBD :)</div>
    </Layout>
  );
};

export default Accounts;
