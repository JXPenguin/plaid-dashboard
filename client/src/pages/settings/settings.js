import React from "react";

import Layout from "../../components/layout";

const Settings = ({ plaidLink }) => {
  return (
    <Layout selected="settings" plaidLink={plaidLink}>
      <div>
        Settings to be added soon, we'll be adding linking and account
        management here in the future :)
      </div>
    </Layout>
  );
};

export default Settings;
